import { Card, PageHeader } from '@/design/ui';

export const AdminPricingPage = () => (
  <div className="space-y-4">
    <PageHeader title="Admin Pricing Controls" subtitle="Plans, one-off listing pricing, featured duration pricing, commission settings." />
    <Card>Editable pricing matrix placeholder για συνδρομές, one-off και pin packages.</Card>
  </div>
);
