import { Card, PageHeader } from '@/design/ui';

export const BuyerDashboardPage = () => (
  <div className="space-y-4">
    <PageHeader title="Buyer / Investor Dashboard" subtitle="Buy request preferences, ROI targets και matching με properties + professionals." />
    <Card>Request templates: buy/invest/erasmus search/long-term rental search.</Card>
  </div>
);
